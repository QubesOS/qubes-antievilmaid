#!/bin/sh
#
# This is a very ugly hack, but I found no other way to ensure that
# our antievilmaid script is executed *before* the LUKS password
# prompt. It is important to ensure the user that the system is 'good'
# (by showing the unsealed secret from the TPM) before expecting
# the user to enter any sensitive info on this machine (e.g. LUKS passphrase)
#

/sbin/anti-evil-maid $@
/sbin/cryptroot-ask.orig $@

